常考:
    *和++ 是同一优先级的,并且是从右到左
        *p++  先取p指向的值,然后将p移动  <=> *(p++) 
        *++p  先将p移动,再取p指向的值  <=> *(++p)
         ++*p  先取值,再将p指向的值自加 
        (*p)++ 由于有括号,先取值,再将指向的值自加
        

    printf函数中的参数从右到左运算

    
    
